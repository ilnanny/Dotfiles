try this, to make portage go deeper:

 

emerge -a -v -p -t --with-bdeps=y --backtrack=30 -D -N -1 portage

 

If that goes well remove -p, and run it for real.

 

the important ones are backtrack, with-bdeps, -D -N, for dpendencies and useflags , and -1.
