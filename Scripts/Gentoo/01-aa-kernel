
## 

eselect kernel list
eselect kernel set 
cd /usr/src/linux

/usr/bin/time -v make KCFLAGS="-march=native" -j "$(nproc)" olddefconfig all

(mountpoint -q /boot || mount /boot) && make install modules_install && grub-mkconfig -o /boot/grub/grub.cfg
emerge -avtq '@module-rebuild'
